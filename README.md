required node and docker

`docker-compose up`

- Crux at http://localhost:3000
- JMX at http://localhost:9010
- Prometheus at http://localhost:9090

clients

`npm install && npm run build`

`npm run repl`
`npm run gen`
`npm run follow`
